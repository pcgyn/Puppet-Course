---

- name: Instalacao de pacotes
  #  yum:name={{item}} state=latest
  #  whith_items:
  #      - "{{ common_packages }}"

  # Outra forma
  #- name: Instalacao de pacotes
  #  yum:name={{item}} state=latest
  #  whith_items:
  #      - tcpdump
  #      - vim
  #      - gcc

  # Mais uma forma
  #- name: Instalacao de pacotes
  #  yum:name={{ common_packages }} state=latest

...
